<!-- Copyright (c) Microsoft Corporation and Contributors. -->
<!-- Licensed under the MIT License. -->

<Page
    x:Class="MyShop.View.OrderHistoryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MyShop.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Margin="20, 20, 20, 20">

        <Button Grid.Row="3" Grid.Column="1" Content="Sort Options">
            <Button.Flyout>
                <MenuFlyout>
                    <MenuFlyoutItem Text="Sort by Day" />
                </MenuFlyout>
            </Button.Flyout>
        </Button>
        
        <ListView x:Name="dataListView" ScrollViewer.HorizontalScrollBarVisibility="Disabled" Width="400" Height="400">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Width="200" Height="100">
                        <!--Create item here-->
                        <TextBlock Text="{Binding ID}" FontWeight="Bold"/>
                        <TextBlock Text="{Binding Name}" Foreground="Red"/>
                        
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
        </ListView>

    </Grid>
</Page>
